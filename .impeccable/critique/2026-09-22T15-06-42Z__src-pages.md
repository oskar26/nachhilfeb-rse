---
target: whole web app except landing page
total_score: 19
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 3
timestamp: 2026-09-22T15-06-42Z
slug: src-pages
---
Method: dual-agent (A: ses_f365d827fffeUEd0Ye2GRZtylJ · B: ses_f365d8242ffe2Q92cublzlDRGW)

## Design Health Score — whole app except Landing (Operate + Read)

| # | Heuristic | Score | Key Issue |
|---|---|---|---|
| 1 | Visibility of System Status | 2 | Feed result count live, Chat read receipts ok; but Feed skeleton missing, saved-search toast-only |
| 2 | Match System / Real World | 3 | Grades 5-EF-Q2, SV-Raum/H310/45min strong; broken by "Social Hub / Matches / 70% Match" jargon |
| 3 | User Control and Freedom | 1 | Fake Chat-löschen (toast to SV), CreateAd forward unguarded, no draft autosave, no Abbrechen |
| 4 | Consistency and Standards | 1 | Two design systems (poster header vs glass app), 4 aliases to Social, 3 price formats, 3 subject names |
| 5 | Error Prevention | 2 | Login rate-limit + U16 gate good; image-URL accepts any http, appointment date no min, SV-Code 5-tap Easter egg |
| 6 | Recognition Rather Than Recall | 2 | SubjectChip colors help; 25 subjects + 19 locations dumped at once, no search |
| 7 | Flexibility and Efficiency | 2 | saveCurrentSearch single-slot localStorage only, no bulk admin actions, no deep-link filters |
| 8 | Aesthetic and Minimalist Design | 1 | Feed: 3 info cards + news before first ad (2-3 viewports); AdDetails 6+ pills before description |
| 9 | Error Recovery | 2 | Empty states have CTAs; field errors toast-only with raw Supabase leaks, native confirm() |
| 10 | Help and Documentation | 3 | ParentGuide/Coaching/rules + price calculator best part; Matching "Relevanz" unexplained, boosted hover-only |
| **Total** | | **19/40** | **Poor — major overhaul required before happy users** |

## Design Specificity Verdict

**LLM assessment: category-interchangeable.** App shell is generic glassmorphism SaaS (`Layout.tsx:70,83,200` gray #f8f9fa, white/90 blur sidebar, pill bottom-nav, `Card.tsx:24` rounded-2xl shadow-soft). FWG content (Bibliothek/SV-Raum/Glaskasten/H310 in `CreateAd.tsx:35-40`, Förderunterricht-Timetable `Feed.tsx:716-751`, Coach-Badge `Profile.tsx:415`) is pasted inside generic chrome, not authored into it. `SiteHeader.tsx:59` black+Anton vs `Layout.tsx:83,175` white glass+Inter are two products. Rename test: replace FWG with Stadtbibliothek — Feed/AdDetails/Chat/Profile/Social still work. Failed. Info pages (`StaticLayout.tsx:18,32` font-display uppercase) use poster language; zero app screens do.

**Deterministic scan: 19 findings, exit 2.** CLI `detect.mjs --json src/pages src/components` (69 files, no narrowing): `gray-on-color` x14, `ai-color-palette` x5, all warning. By file: Feed.tsx x4 (562 gray on blue x2, 702 indigo gradient x2), admin/AdminReports.tsx x4, AdminAnalytics.tsx x3, Impressum.tsx x2, AdminUsers.tsx x2, AdminNews/AdminOverview/CoachPanel/Layout x1 each. Zero hits in `ui/*` and SiteHeader/StaticLayout. Separate Landing-only run: [] clean. Detector agrees with H4/H8 (inconsistent grays on colored bg, indigo/purple gradients vs poster ban on gradients) and caught what LLM underweighted: `Layout.tsx:148` text-gray-900 on bg-amber-900, `CoachPanel.tsx:472` gray-600 on green-100, admin red-50/red-100/red-500 gray-text combos.

**Visual overlays: no Human-tab overlay (headless only).** Browser automation WAS available (Python Playwright 1.63 + chromium-1243). Mutable preflight true on all 5 routes, `detect.js` via live-server :8400 injected, overlay:true in headless. No authenticated session so Feed/admin/coach (ProtectedRoute/AdminRoute) redirected `/#/` -> `/#/welcome` (out of scope, 17 hits ignored). Unauthenticated corroboration: `/#/login` 3 patterns (thin-border-wide-shadow, line-length ~95, skipped-heading h1->h3), `/#/coaching` 4 patterns (dark-glow #facc15, 10px SV-text, all-caps-body x2, inter 68%), `/#/eltern-leitfaden` 5 patterns /9 lines (+ line-length ~90, cramped-padding 0px, kicker-above-heading), `/#/impressum` 6 patterns /7 lines (+ gray-on-color matches CLI Impressum.tsx:88, thin-border-wide-shadow). Shared-chrome repeats (dark-glow, 10px text, 33-char caps) fire identically on 3 pages = single header/footer defect, not 3.

## Overall Impression

Trust copy is strong, trust hierarchy is inverted. Price calculator, badges, parent linking are genuinely school-authored. Everything around them — Feed wall, 6-step wizard without save, tiny gray safety links at the exact moment safety matters, two visual languages — makes a 5th-grader and her parents work harder than the pause allows. Biggest opportunity: make Feed find Mathe in 10 seconds and make Verifizierung/Kontakt/Melden unmissable.

## What's Working

1. **Price fairness calculator (`CreateAd.tsx:730-769`).** Effective hourly + tiered advice (emerald <9, sky 9-14 "ca. 12€/h Richtwert", amber, rose >18). No generic marketplace does this. Keep, surface in Feed.
2. **Trust badges where it counts.** Feed.tsx:845-853 Verifiziert + Coach (Award), AdDetails.tsx:233, PublicProfile.tsx:164-173, Matching.tsx:250-254 — same green/amber semantics with title/tooltip. Scannable for parents.
3. **Parent linking + U16 consent thought through.** ParentDashboard.tsx:236-240 3-step code, Settings.tsx:334-404 role-aware cards, ParentGuide.tsx:187-199, Login.tsx:554-575 U16 gate. Rare for school app to do read-only oversight without exposing chat.

## Priority Issues

**[P0] CreateAd 6-step wizard has no guardrails or save — `CreateAd.tsx:17-24,168-171,283-294`**
Why: Core Bietende conversion fails on mobile; reload = total loss. Jordan (11yo) abandons at Step 2 (19 locations).
Fix: Enforce `isStepValid()` on Next (disable + inline reason), autosave `formData+currentStep` to `fwg_draft_ad`, chunk subjects with search, default duration Egal, add Abbrechen/Draft.
Suggested command: /impeccable clarify

**[P1] Feed buries marketplace under info walls — `Feed.tsx:648-781,818`**
Why: Operate users come to find Mathe NOW (5-min pause). Coaching + Förderunterricht 4x4 table + Eltern cards before `sortedAds.map` = first ad 2-3 viewports down. Detector indigo gradients (702) + gray-on-blue (562) live in this wall.
Fix: Collapse all three by default (reuse showBanners), move timetable to /coaching, keep one slim Infos row. Measure scroll-to-first-card.
Suggested command: /impeccable distill

**[P1] Contact reveal + reporting feel unsafe at high-stakes moment — `AdDetails.tsx:243-251,262-277,330-334`, `Chat.tsx:283-323,312-321`**
Why: PRODUCT principle #1 Vertrauen vor Reichweite. Accepted-state private fallback is tiny gray (`bg-gray-55/50` typo), Report is `text-gray-400` 12px, Melden behind 3-dot MoreVertical, delete is fake toast to SV.
Fix: Promote Report to outlined Flag button next to Share/Favorite; accepted-state safety strip (Bibliothek treffen, Eltern informieren); replace fake delete or remove.
Suggested command: /impeccable harden

**[P1] Navigation speaks two languages — `Layout.tsx:57-65,105-115`, `App.tsx:143-146`, `Social.tsx:15-23,30-32,73`**
Why: Mobile "Social/Optionen" vs desktop "Anfragen/Matches/Merkliste/Einstellungen"; 4 aliases to one Social with ?tab+path dual state; "Social Hub" jargon. Casey taps Social expecting chat, lands on Requests; shared links diverge.
Fix: Unify to Anfragen/Matches/Merkliste everywhere, single source truth (search param only), rename Social Hub -> Anfragen & Matches, remove aliases.
Suggested command: /impeccable shape

**[P2] App vs Info visual split breaks trust + micro-a11y fails — `SiteHeader.tsx:59` vs `Layout.tsx:83,175`, `StaticLayout.tsx:32` vs `Feed.tsx:422`**
Why: Poster landing -> generic SaaS app = "bin ich noch beim FWG?". Detector + browser corroborate: dark-glow #facc15, 10px SV-text (undersized-ui-text), all-caps-body, inter 68-93% (overused-font), skipped-heading login h1->h3, line-length ~95, cramped-padding, thin-border-wide-shadow.
Fix: Port poster tokens sparingly: font-display h1 in Feed/Profile/ParentDashboard, black section headers, yellow Verifiziert stamp echo, keep cards white. Fix 10px->12px min, heading order, line-length <80ch.
Suggested command: /impeccable colorize

## Persona Red Flags

**Casey (distracted mobile, pause, one-hand) — Feed->Anfrage->Chat:** Quick chips `Feed.tsx:484-500` px-3 py-1 (~28px) miss-taps; Filter/Merken `Feed.tsx:453-464` h-8 text-xs fat-finger pair; AdDetails top-right ghost icon row (Duplizieren/Share/Heart 20px, no aria-label) `AdDetails.tsx:137-147`; post-Anfrage toast-only + yellow Warte box = checks phone 10x; Chat edit/delete `opacity-0 group-hover:opacity-100` unreachable on touch `Chat.tsx:432-449`.

**Jordan (first-timer Klasse 6 + Eltern) — Register->Verify->CreateAd:** Register `Login.tsx:476-589` 9+ fields one scroll, no stepper; SV-Code looks optional (dashed, no *) with no "woher?" link `Login.tsx:577-588`; lock wall `CreateAd.tsx:298-314` CTA only "Zum Profil" (wrong dest) -> must discover 5-tap secret `Settings.tsx:519-575`; Step1 25 subjects no search `CreateAd.tsx:510-542`, Step2 19 locations `553-599`.

**Sam (keyboard/SR/contrast):** Positive: skip-link `Layout.tsx:71-81`, Dialog Escape `Dialog.tsx:32-34`, tablist arrow keys `Social.tsx:45-66,86-96`, Input focus ring. Failures: SubjectChip in CreateAd no aria-pressed (SR hears no selected state) `SubjectChip.tsx:112-126` vs Feed `Feed.tsx:484-500` ok; boosted title-only `Feed.tsx:831` invisible touch/SR; Matching score div soup no aria-label `Matching.tsx:218-231`; color input no label `Profile.tsx:338-350`; 10px green-700/9px badges fail low-vision `Feed.tsx:846`, `Favorites.tsx:135`; Chat doodle SVG no reduced-motion guard `Chat.tsx:328`.

## Minor Observations

- AdDetails H1 `MATHEMATIK - Max` shouty/impersonal; use short_description (`AdDetails.tsx:157`).
- `VB` no tooltip (Feed + AdDetails:128) — Jordan doesn't know Verhandlungsbasis.
- 9-10px badges unreadable (`Favorites.tsx:135`, `Requests.tsx:80`).
- Double tab bars: Requests Eingang/Gesendet inside Social tabs (`Requests.tsx:76` in `Social.tsx:81`) — confusing focus order.
- Matching alias map (spanisch/geographie/politik/wirtschaft `Matching.tsx:72-128`) not in SubjectChip taxonomy — phantom matches.
- ParentDashboard checkbox w-4 vs Settings ToggleSwitch 44px — inconsistent + small (`ParentDashboard.tsx:307-312`).
- Admin `confirm('Anzeige endgültig löschen?')` native, no undo (`Admin/Dashboard.tsx:116`).
- Chat Termin-Ort hardcoded `FWG Bibliothek/Schulgelände` contradicts ad locations (`Chat.tsx:65`); ICS parse via split('\n')[1] fragile (`Chat.tsx:394-412`).
- PublicProfile `is_hidden=false` vs Feed `is_active=true` mismatch (`PublicProfile.tsx:67`).
- Settings "Account löschen" files support ticket, mislabeled, no tracker (`Settings.tsx:173-191`).

## Questions to Consider

- If verification happens in person im SV-Raum, why does the Operate flow hide SV-Raum hours/map/queue and hide code entry behind a 5-tap Easter egg?
- Who is the 20% Match (`Matching.tsx:643` floor) for — a 5th-grader who needs hope, or an algorithm that needs inventory?
- If a parent asks "where is my child meeting whom?", can ParentDashboard answer it, or only counts while Chat stays invisible by design?
- What breaks if Förderunterricht/Coaching/Eltern cards leave Feed tomorrow — and what does it say that Feed has no FWG identity without them?
